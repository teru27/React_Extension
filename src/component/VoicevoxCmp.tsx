import { type FC } from "react";
import "./modal.css";

interface voicevoxCmpType {}

export const VoicevoxCmp: FC<voicevoxCmpType> = (props: any) => {
  return (
    <div>
      <strong>voicevoxCmpType</strong>
    </div>
  );
};
